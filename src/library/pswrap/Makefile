include ../../Makefile.conf

CFLAGS += 
IFLAGS += 
LFLAGS += 

MODULE_NAME = PSWRAP

SOURCES = pswrap.cpp

PUBLIC_INCLUDES = pswrap.h
PUBLIC_LIBRARIES = libpswrap.a
PUBLIC_BINARIES = 

TARGETS = libpswrap.a

# rules

libpswrap.a:	pswrap.o

include ../../Makefile.rules
