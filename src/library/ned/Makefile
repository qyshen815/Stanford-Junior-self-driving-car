include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = NED

SOURCES = bil.cpp

PUBLIC_INCLUDES = bil.h
PUBLIC_LIBRARIES = libbil.a
PUBLIC_BINARIES = 

TARGETS = libbil.a

# rules

libbil.a:	bil.o

include ../../Makefile.rules
