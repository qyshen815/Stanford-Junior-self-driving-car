include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = TRAJECTORY

SOURCES = trajectory.cpp

PUBLIC_INCLUDES = trajectory.h
PUBLIC_LIBRARIES = libtrajectory.a
PUBLIC_BINARIES = 

TARGETS = libtrajectory.a

# rules

libtrajectory.a:	trajectory.o

include ../../Makefile.rules
