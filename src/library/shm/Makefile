include ../../Makefile.conf

CFLAGS += 
CXXFLAGS +=

MODULE_NAME = SHM_WRAPPER

SOURCES = 		shm_wrapper.cpp

PUBLIC_INCLUDES = 	shm_wrapper.h
PUBLIC_LIBRARIES = 	libshm_wrapper.a
PUBLIC_BINARIES =

TARGETS = 		libshm_wrapper.a

# rules

libshm_wrapper.a: 	shm_wrapper.o

include ../../Makefile.rules
