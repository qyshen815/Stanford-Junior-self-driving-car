include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = FSM

SOURCES = fsm.cpp

PUBLIC_INCLUDES = fsm.h
PUBLIC_LIBRARIES = libfsm.a
PUBLIC_BINARIES = 

TARGETS = libfsm.a

# rules

libfsm.a:	fsm.o

include ../../Makefile.rules
