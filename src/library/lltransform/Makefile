include ../../Makefile.conf

CFLAGS += 
IFLAGS += 
LFLAGS += 

MODULE_NAME = LLTRANSFORM

SOURCES = lltoutm.cpp lltospcs.cpp

PUBLIC_INCLUDES = lltransform.h
PUBLIC_LIBRARIES = liblltransform.a liblltospcs.a
PUBLIC_BINARIES = 

TARGETS = liblltransform.a

# rules

liblltransform.a:	lltoutm.o lltospcs.o

include ../../Makefile.rules
