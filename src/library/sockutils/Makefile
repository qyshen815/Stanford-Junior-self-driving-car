include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = SOCKUTILS

SOURCES = sockutils.cpp

PUBLIC_INCLUDES = sockutils.h
PUBLIC_LIBRARIES = libsockutils.a
PUBLIC_BINARIES = 

TARGETS = libsockutils.a

# rules

libsockutils.a:		sockutils.o

include ../../Makefile.rules
