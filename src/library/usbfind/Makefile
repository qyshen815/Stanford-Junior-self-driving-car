include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = USBFIND

SOURCES = usbfind.cpp

PUBLIC_INCLUDES = usbfind.h
PUBLIC_LIBRARIES = libusbfind.a
PUBLIC_BINARIES = 

TARGETS = libusbfind.a

# rules

libusbfind.a:	usbfind.o

include ../../Makefile.rules
