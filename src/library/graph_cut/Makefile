include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = 		GRAPH_CUT

SOURCES = 		graph_cut.cpp 

PUBLIC_INCLUDES = 	graph_cut.h bit_vector.h
PUBLIC_LIBRARIES = 	libgraph_cut.a
PUBLIC_BINARIES = 

TARGETS = 		libgraph_cut.a

libgraph_cut.a:		graph_cut.o

include ../../Makefile.rules