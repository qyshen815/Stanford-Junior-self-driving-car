include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

LINK = g++

MODULE_NAME = PROJECT

SOURCES = project.cpp 

PUBLIC_INCLUDES = project.h
PUBLIC_LIBRARIES = libproject.a
PUBLIC_BINARIES = 

TARGETS = libproject.a 

# rules

libproject.a:	project.o

include ../../Makefile.rules
