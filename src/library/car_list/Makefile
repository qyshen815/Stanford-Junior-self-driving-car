include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = 		CAR_LIST

SOURCES = 		car_list.cpp 

PUBLIC_INCLUDES = 	car_list.h
PUBLIC_LIBRARIES = 	libcar_list.a
PUBLIC_BINARIES = 

TARGETS = 		libcar_list.a

libcar_list.a:		car_list.o

include ../../Makefile.rules
