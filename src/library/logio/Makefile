include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = 		LOGIO

SOURCES = 		logio.cc

PUBLIC_INCLUDES = 	logio.h 
PUBLIC_LIBRARIES = 	liblogio.a
PUBLIC_BINARIES = 

TARGETS = 		liblogio.a

# rules

liblogio.a:		logio.o

include ../../Makefile.rules
