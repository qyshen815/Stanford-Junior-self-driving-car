include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = GRID

SOURCES = grid.cpp

PUBLIC_INCLUDES = grid.h
PUBLIC_LIBRARIES = libgrid.a
PUBLIC_BINARIES = 

TARGETS = libgrid.a

# rules

libgrid.a:	grid.o

include ../../Makefile.rules
