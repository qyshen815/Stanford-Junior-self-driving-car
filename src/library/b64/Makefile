include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = B64

SOURCES = b64.cpp 

PUBLIC_INCLUDES = b64.h
PUBLIC_LIBRARIES = libb64.a
PUBLIC_BINARIES = 

TARGETS = libb64.a 

# rules

libb64.a:	b64.o

include ../../Makefile.rules
