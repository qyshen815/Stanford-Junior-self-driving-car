include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

MODULE_NAME = LIBRT_STUB

SOURCES = librt_stub.cpp         
PUBLIC_INCLUDES = 
PUBLIC_LIBRARIES = librt.a
PUBLIC_BINARIES = 

TARGETS = librt.a

# rules
librt.a: librt_stub.o
 
include ../../Makefile.rules
