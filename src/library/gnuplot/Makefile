include ../../Makefile.conf

CFLAGS +=
IFLAGS += 
LFLAGS += 

LINK = g++

MODULE_NAME = GNUPLOT

SOURCES = gnuplot.cpp

PUBLIC_INCLUDES = gnuplot.h
PUBLIC_LIBRARIES = libgnuplot.a
PUBLIC_BINARIES = 

TARGETS = libgnuplot.a

# rules

libgnuplot.a:	gnuplot.o

include ../../Makefile.rules
